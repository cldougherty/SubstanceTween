	public enum AnimationType  {PingPong , Loop}; // in future each variable will have the option to animate differently. 
	public AnimationType animationType;
	public bool pingPong; //but for now all variables will animate the same way.
	public List<AnimationType> variableAnimationType = new List<AnimationType>();




public void OnGUI()
{

	if(propType == ProceduralPropertyType.Float)
					{
						 
						if (variableAnimationType.Count < objProperties.Length)
						variableAnimationType.Add (AnimationType.Loop);
						variableAnimationType[i] = (AnimationType)  EditorGUILayout.EnumPopup ( variableAnimationType[i]);

					}


}